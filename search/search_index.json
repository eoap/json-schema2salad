{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"json-schema2salad","text":"<p><code>json-schema2salad</code> translates JSON Schema documents into a practical subset of Schema Salad, the schema language used by the Common Workflow Language ecosystem.</p> <p>The documentation is organized with the Diataxis approach:</p> <ul> <li>Tutorials help you learn the tool by making   one successful conversion.</li> <li>How-to guides help you complete specific   conversion tasks.</li> <li>Reference gives exact command-line and conversion-rule   details.</li> <li>Explanation describes the conversion model,   trade-offs, and current scope.</li> </ul> <p>Development status</p> <p>This tool is still being refined. Unsupported or lossy cases are preserved as far as possible, and the converter records warnings when it must fall back to a broader <code>Any</code> type. The current behavior should be treated as an implemented conversion policy, not yet as a full JSON Schema to Schema Salad specification.</p>"},{"location":"#start-here","title":"Start here","text":"<p>If you are new to the tool, begin with Convert your first JSON Schema.</p> <p>If you already know what you need to do, go straight to Convert JSON Schema documents.</p> <p>If you need exact behavior, use the command line reference and conversion rules reference.</p>"},{"location":"explanation/conversion-model/","title":"Conversion model and scope","text":"<p><code>json-schema2salad</code> is an ongoing work in progress for translating JSON Schema documents into Schema Salad documents.</p> <p>The project is intentionally small, explicit, and evolving. It does not try to be a complete semantic mirror of every JSON Schema keyword. Instead, it focuses on producing useful Salad schema documents from the common structural features that appear in EOAP-style schemas: objects, fields, primitive values, arrays, enums, references, definitions, documentation, and selected string formats.</p>"},{"location":"explanation/conversion-model/#why-the-output-is-a-salad-graph","title":"Why the output is a Salad graph","text":"<p>The generated document is a Salad document with a <code>$namespaces</code> section and a <code>$graph</code> containing records, enums, and graph-level <code>$import</code> directives when external Salad schemas are required.</p> <p>For example, recognized JSON Schema <code>format</code> values for strings can reference the EOAP <code>string_format</code> schema through a namespace such as <code>string_format</code>, producing types like <code>string_format:Date</code>.</p> <p>This keeps generated records close to their type graph while still declaring external Salad type libraries in the way Salad expects.</p>"},{"location":"explanation/conversion-model/#what-the-converter-optimizes-for","title":"What the converter optimizes for","text":"<p>The converter is best understood as a converter for the structural center of JSON Schema, not a validator and not a complete preservation layer for every validation keyword.</p> <p>The goal is to make the useful path pleasant: take a JSON Schema model, convert its shape into a readable Salad type graph, keep documentation close to the generated fields, import external Salad type libraries in the expected place, and leave clear warnings where the conversion still needs human review.</p>"},{"location":"explanation/conversion-model/#what-is-outside-the-current-scope","title":"What is outside the current scope","text":"<p>Constraints such as numeric ranges, regular expression patterns, conditional validation, dependency rules, and unevaluated property semantics are currently outside the implemented mapping.</p> <p>Unsupported or lossy cases are preserved as far as possible. When the converter cannot represent a JSON Schema construct precisely, it records a warning and falls back to a broader <code>Any</code> type.</p> <p>For exact behavior, see the conversion rules reference.</p>"},{"location":"how-to/convert-schemas/","title":"Convert JSON Schema documents","text":"<p>Use these guides when you already know the conversion task you want to complete.</p>"},{"location":"how-to/convert-schemas/#convert-a-local-schema","title":"Convert a local schema","text":"<p>Pass one local JSON or YAML schema file and choose the merged Salad output path:</p> <pre><code>json-schema2salad --output build/model.salad.yaml schemas/model.json\n</code></pre> <p>Local paths are expanded and normalized before conversion starts. Parent directories for the output file are created automatically when needed.</p>"},{"location":"how-to/convert-schemas/#convert-a-remote-schema","title":"Convert a remote schema","text":"<p>Use an <code>http://</code> or <code>https://</code> source:</p> <pre><code>json-schema2salad \\\n  --output build/remote.salad.yaml \\\n  https://example.org/schemas/root.yaml\n</code></pre> <p>Remote sources are fetched with redirects enabled. Responses with an <code>application/json</code> content type are parsed as JSON; other responses are parsed as YAML.</p>"},{"location":"how-to/convert-schemas/#merge-several-schemas","title":"Merge several schemas","text":"<p>Pass several independent sources. They are emitted into the same Salad document:</p> <pre><code>json-schema2salad \\\n  --output catalog.salad.yaml \\\n  schemas/product.json \\\n  schemas/order.json \\\n  schemas/customer.json\n</code></pre> <p>Shared external schemas are de-duplicated across all inputs. If two inputs both reference the same external schema, the shared types are emitted once.</p>"},{"location":"how-to/convert-schemas/#inline-an-external-reference","title":"Inline an external reference","text":"<p>Use a normal JSON Schema <code>$ref</code> from one schema to another:</p> <pre><code>{\n  \"title\": \"Root\",\n  \"type\": \"object\",\n  \"properties\": {\n    \"child\": { \"$ref\": \"child.json\" }\n  },\n  \"required\": [\"child\"]\n}\n</code></pre> <p>Then convert the root schema:</p> <pre><code>json-schema2salad --output merged.salad.yaml root.json\n</code></pre> <p>The converter loads <code>child.json</code>, converts it, and places both the root type and the referenced type in <code>merged.salad.yaml</code>.</p> <p>External definition references such as <code>shared.json#/$defs/Address</code> are also resolved when they point to top-level definitions that can be planned and emitted.</p>"},{"location":"how-to/convert-schemas/#preserve-recognized-string-formats","title":"Preserve recognized string formats","text":"<p>Use a supported JSON Schema <code>format</code> value on a string field:</p> <pre><code>{\n  \"title\": \"Root\",\n  \"type\": \"object\",\n  \"properties\": {\n    \"published\": {\n      \"type\": \"string\",\n      \"format\": \"date\"\n    }\n  },\n  \"required\": [\"published\"]\n}\n</code></pre> <p>The generated Salad document imports the EOAP string format schema and qualifies the field type with the generated namespace:</p> <pre><code>$namespaces:\n  sld: https://w3id.org/cwl/salad#\n  string_format: https://raw.githubusercontent.com/eoap/schemas/refs/heads/main/string_format.yaml#\n$graph:\n  - $import: https://raw.githubusercontent.com/eoap/schemas/refs/heads/main/string_format.yaml\n  - type: record\n    name: Root\n    fields:\n      - name: published\n        type: string_format:Date\n</code></pre>"},{"location":"how-to/convert-schemas/#review-conversion-warnings","title":"Review conversion warnings","text":"<p>Read the command output after each run. Warnings are logged when a JSON Schema construct cannot be represented precisely and the converter falls back to a broader type such as <code>Any</code>.</p> <p>Warnings are also collected into the generated Salad document comment so the output can be reviewed later.</p> <p>For exact supported behavior, see the conversion rules reference.</p>"},{"location":"reference/cli/","title":"Command line reference","text":"<p>The <code>json-schema2salad</code> command converts one or more JSON Schema sources into a single Schema Salad document.</p>"},{"location":"reference/cli/#synopsis","title":"Synopsis","text":"<pre><code>json-schema2salad --output OUTPUT SOURCE [SOURCE ...]\n</code></pre> <p>The command is installed from the Python package entry point:</p> <pre><code>pip install json-schema2salad\n</code></pre>"},{"location":"reference/cli/#arguments","title":"Arguments","text":"<p><code>SOURCE</code> : One or more JSON Schema sources to convert. A source can be a local path, a   <code>file://</code> URI, or an <code>http://</code> / <code>https://</code> URI. Local paths are expanded and   normalized to absolute paths before conversion starts.</p>"},{"location":"reference/cli/#options","title":"Options","text":"<p><code>--output OUTPUT</code> : Required. The file where the merged Salad document will be written. Parent   directories are created automatically when needed.</p>"},{"location":"reference/cli/#source-loading","title":"Source loading","text":"<p>Local files are parsed with YAML loading, which also accepts JSON because JSON is a subset of YAML.</p> <p>Remote <code>http://</code> and <code>https://</code> sources are fetched with redirects enabled. If the response declares an <code>application/json</code> content type, it is parsed as JSON; otherwise, the response body is parsed as YAML.</p> <p>Resolved schema roots must be JSON objects.</p>"},{"location":"reference/cli/#reference-resolution","title":"Reference resolution","text":"<p>The CLI recursively inlines supported external references into the same output document. Relative references are resolved from the schema file that contains the <code>$ref</code>.</p> <p>References to document roots and top-level definitions are supported. Local definitions such as <code>#/$defs/Thing</code> and <code>#/definitions/Thing</code> are converted to stable Salad type names. External definition references such as <code>shared.json#/$defs/Address</code> are resolved by loading the external document and linking the field to the generated Salad type.</p> <p>If a referenced schema has already been processed, it is reused rather than loaded and emitted again.</p>"},{"location":"reference/cli/#run-flow","title":"Run flow","text":"<p>For each schema, the merger:</p> <ol> <li>Loads the schema document.</li> <li>Plans stable Salad type names before conversion.</li> <li>Converts the schema into Salad graph entries.</li> <li>Recursively resolves supported external <code>$ref</code> values.</li> <li>De-duplicates schemas and generated types that were already seen.</li> <li>Writes the final merged Salad document to <code>--output</code>.</li> </ol>"},{"location":"reference/cli/#output-structure","title":"Output structure","text":"<p>The CLI writes YAML using the Schema Salad document shape used by the converter:</p> <pre><code>$namespaces:\n  sld: https://w3id.org/cwl/salad#\n$graph:\n  - type: record\n    name: Root\n    fields:\n      - name: id\n        type: string\n</code></pre> <p>When an external Salad schema is required, it is declared as a graph-level <code>$import</code> and qualified through <code>$namespaces</code>:</p> <pre><code>$namespaces:\n  sld: https://w3id.org/cwl/salad#\n  string_format: https://raw.githubusercontent.com/eoap/schemas/refs/heads/main/string_format.yaml#\n$graph:\n  - $import: https://raw.githubusercontent.com/eoap/schemas/refs/heads/main/string_format.yaml\n  - type: record\n    name: Root\n    fields:\n      - name: published\n        type: string_format:Date\n</code></pre>"},{"location":"reference/cli/#logging-and-warnings","title":"Logging and warnings","text":"<p>The command logs the main phases of the run:</p> <ul> <li>normalized input sources;</li> <li>output path;</li> <li>recursive schema inlining;</li> <li>warnings produced during conversion;</li> <li>success or failure;</li> <li>total runtime and finish timestamp.</li> </ul> <p>Warnings are also collected into the generated Salad document comment.</p>"},{"location":"reference/cli/#boundaries","title":"Boundaries","text":"<p>The CLI follows the same boundaries as the converter itself. It focuses on structural conversion and recursive merging, not complete preservation of every JSON Schema validation keyword.</p> <p>Unsupported external reference fragments raise a conversion error. External references are expected to point to a document root or to a top-level definition that can be planned and emitted.</p>"},{"location":"reference/conversion-rules/","title":"Conversion rules reference","text":"<p>The converter currently applies these rules.</p>"},{"location":"reference/conversion-rules/#document-shape","title":"Document shape","text":"<p>Output is serialized as a Salad document with <code>$namespaces</code> and <code>$graph</code>. External Salad dependencies are not emitted under <code>$schemas</code>; they are imported once through <code>$import</code> entries inside <code>$graph</code>.</p>"},{"location":"reference/conversion-rules/#namespaces-and-imports","title":"Namespaces and imports","text":"<p>The built-in <code>sld</code> namespace points to <code>https://w3id.org/cwl/salad#</code>. External schema namespaces are derived from the imported file name, so <code>string_format.yaml</code> becomes <code>string_format</code>, and the namespace value ends with <code>#</code>.</p>"},{"location":"reference/conversion-rules/#object-schemas","title":"Object schemas","text":"<p>JSON Schema objects and schemas with <code>properties</code> become Salad <code>record</code> types. Required properties remain required. Optional properties are represented as nullable unions by adding <code>null</code>.</p>"},{"location":"reference/conversion-rules/#primitive-types","title":"Primitive types","text":"<p>JSON Schema primitive types map to Salad primitive types as follows:</p> JSON Schema type Salad type <code>string</code> <code>string</code> <code>integer</code> <code>int</code> <code>number</code> <code>float</code> <code>boolean</code> <code>boolean</code> <code>null</code> <code>null</code>"},{"location":"reference/conversion-rules/#string-formats","title":"String formats","text":"<p>Recognized string formats, such as <code>date</code>, <code>date-time</code>, <code>uri</code>, <code>uuid</code>, <code>email</code>, <code>ipv4</code>, and <code>ipv6</code>, are mapped to named types from the EOAP string format Salad schema. References are qualified with the generated namespace, for example <code>string_format:Date</code> or <code>string_format:URI</code>.</p>"},{"location":"reference/conversion-rules/#arrays","title":"Arrays","text":"<p>JSON Schema arrays are emitted as Salad array types, with <code>items</code> recursively converted using the same type rules.</p>"},{"location":"reference/conversion-rules/#enums","title":"Enums","text":"<p>JSON Schema <code>enum</code> values become Salad <code>enum</code> definitions with stringified symbols.</p>"},{"location":"reference/conversion-rules/#references","title":"References","text":"<p>Local <code>$ref</code> values into <code>#/$defs/...</code> and <code>#/definitions/...</code> are resolved to emitted Salad type names. The CLI merger can also recursively load external referenced schemas and inline their record or enum definitions into the final document.</p>"},{"location":"reference/conversion-rules/#definitions","title":"Definitions","text":"<p><code>$defs</code> and <code>definitions</code> are predeclared so references can point to stable generated names. Object-like definitions become records, enum definitions become enums, and unsupported definition shapes are wrapped in a record with a required <code>value</code> field.</p>"},{"location":"reference/conversion-rules/#compositions","title":"Compositions","text":"<p><code>oneOf</code> and <code>anyOf</code> become Salad unions. <code>allOf</code> is handled for object-like schemas by using <code>extends</code> when branches are references and by flattening inline object branches into a merged record.</p>"},{"location":"reference/conversion-rules/#names","title":"Names","text":"<p>Record names are derived from schema titles when available, falling back to reference paths, field names, or source file hints. Names are sanitized for Salad compatibility and de-duplicated when the same preferred name appears more than once.</p>"},{"location":"reference/conversion-rules/#documentation","title":"Documentation","text":"<p>JSON Schema <code>description</code> is carried into Salad <code>doc</code> fields. When no description is present, <code>title</code> can be used as documentation. Example values are appended to field and enum docs when available.</p>"},{"location":"reference/conversion-rules/#merging-behavior","title":"Merging behavior","text":"<p>When the CLI receives multiple sources, or a source references shared external schemas, it emits a single merged Salad document, de-duplicating schema loads and type definitions by source reference where possible.</p>"},{"location":"reference/conversion-rules/#fallbacks-and-warnings","title":"Fallbacks and warnings","text":"<p>Unsupported type variants and unsupported non-object <code>allOf</code> branches degrade to <code>Any</code>, with warnings collected into the generated document comment.</p>"},{"location":"tutorials/first-conversion/","title":"Convert your first JSON Schema","text":"<p>In this tutorial, we will convert a small JSON Schema document into a Schema Salad document and check the generated output.</p>"},{"location":"tutorials/first-conversion/#install-the-command","title":"Install the command","text":"<p>Install the package so the <code>json-schema2salad</code> command is available:</p> <pre><code>pip install json-schema2salad\n</code></pre>"},{"location":"tutorials/first-conversion/#create-a-schema","title":"Create a schema","text":"<p>Create a file named <code>person.schema.json</code>:</p> <pre><code>{\n  \"title\": \"Person\",\n  \"type\": \"object\",\n  \"description\": \"A person record.\",\n  \"properties\": {\n    \"name\": {\n      \"type\": \"string\",\n      \"description\": \"The person's display name.\"\n    },\n    \"age\": {\n      \"type\": \"integer\"\n    }\n  },\n  \"required\": [\"name\"]\n}\n</code></pre> <p>This schema has one required field, <code>name</code>, and one optional field, <code>age</code>.</p>"},{"location":"tutorials/first-conversion/#run-the-converter","title":"Run the converter","text":"<p>Run the command with the schema as the source and choose an output file:</p> <pre><code>json-schema2salad --output person.salad.yaml person.schema.json\n</code></pre> <p>The command writes one Schema Salad document to <code>person.salad.yaml</code>.</p>"},{"location":"tutorials/first-conversion/#inspect-the-result","title":"Inspect the result","text":"<p>Open <code>person.salad.yaml</code>. The generated document should look like this:</p> <pre><code>$namespaces:\n  sld: https://w3id.org/cwl/salad#\n$graph:\n- type: record\n  name: Person\n  fields:\n  - name: name\n    type: string\n    doc: The person's display name.\n  - name: age\n    type:\n    - 'null'\n    - int\n  doc: A person record.\n</code></pre> <p>Notice that the required <code>name</code> field remains a plain <code>string</code>, while the optional <code>age</code> field becomes a union with <code>null</code>.</p>"},{"location":"tutorials/first-conversion/#convert-a-formatted-string","title":"Convert a formatted string","text":"<p>Add a <code>published</code> field with the JSON Schema <code>date</code> format:</p> <pre><code>{\n  \"title\": \"Person\",\n  \"type\": \"object\",\n  \"properties\": {\n    \"name\": { \"type\": \"string\" },\n    \"published\": {\n      \"type\": \"string\",\n      \"format\": \"date\"\n    }\n  },\n  \"required\": [\"name\", \"published\"]\n}\n</code></pre> <p>Run the converter again:</p> <pre><code>json-schema2salad --output person.salad.yaml person.schema.json\n</code></pre> <p>The output now includes the EOAP string format schema as a graph-level import and uses the generated namespace in the field type:</p> <pre><code>$namespaces:\n  sld: https://w3id.org/cwl/salad#\n  string_format: https://raw.githubusercontent.com/eoap/schemas/refs/heads/main/string_format.yaml#\n$graph:\n- $import: https://raw.githubusercontent.com/eoap/schemas/refs/heads/main/string_format.yaml\n- type: record\n  name: Person\n  fields:\n  - name: name\n    type: string\n  - name: published\n    type: string_format:Date\n</code></pre> <p>You have now converted a JSON Schema object into a Salad record, checked an optional field, and seen how recognized string formats become named Salad types.</p> <p>Next, use the how-to guides for task-focused commands, or the conversion rules reference for the full implemented mapping.</p>"}]}